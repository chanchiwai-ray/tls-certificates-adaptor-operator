# 001. Add dependency, create `constants.py`, rename charm class

## What

Add `charmlibs-interfaces-tls-certificates` to the `dependencies` list in `pyproject.toml` and regenerate `uv.lock`. Create `src/constants.py` with the project-wide string constants defined in the spec (Juju Secret label prefix, relation names, CSR hash algorithm). Rename the `Charm` class to `TLSCertificateAdaptorCharm` in `src/charm.py` and update the corresponding reference in `tests/unit/test_base.py`.

## Why

The dependency is required to use `TLSCertificatesRequiresV4` and `CertificateRequestAttributes` from charmlibs (spec: New-Interface Relation Data Format). Named constants prevent magic strings scattered throughout the codebase (general-coding-style instructions). The class rename aligns with Juju charm naming conventions.

## Acceptance Criteria

- [ ] `charmlibs-interfaces-tls-certificates` appears in `pyproject.toml` `dependencies` and is resolvable via `uv lock`.
- [ ] `src/constants.py` exists and defines at minimum `JUJU_SECRET_LABEL_PREFIX`, `OLD_INTERFACE_RELATION_NAME`, and `UPSTREAM_RELATION_NAME`.
- [ ] `src/charm.py` exports `TLSCertificateAdaptorCharm` (not `Charm`).
- [ ] `tox -e unit` passes with 100% coverage of `src/charm.py`.

## Files

- `pyproject.toml` — add dependency
- `uv.lock` — regenerated by `uv lock`
- `src/constants.py` — new file
- `src/charm.py` — rename class
- `tests/unit/test_base.py` — update class reference

## Notes

- Pin to the same version already used in vault-k8s to avoid library churn. Check the latest release on Charmhub before pinning.
- `JUJU_SECRET_LABEL_PREFIX = "tls-adaptor-"` (spec: Key Management).
- `OLD_INTERFACE_RELATION_NAME = "certificates"` and `UPSTREAM_RELATION_NAME = "certificates-upstream"` (spec: Charm Metadata).
